<Page
    x:Class="CoolWSL.App.Views.DistroPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:diagnostics="using:CoolWSL.Diagnostics.Models"
    xmlns:core="using:CoolWSL.Core.Models"
    xmlns:controls="using:CoolWSL.App.Views.Controls"
    xmlns:viewmodels="using:CoolWSL.App.ViewModels"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    UseLayoutRounding="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="DistroHeaderContentHost" MaxWidth="{StaticResource WidePageContentMaxWidth}" HorizontalAlignment="Stretch">
        <StackPanel Padding="12,32,12,8" Spacing="12">
            <Grid ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Spacing="6">
                    <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="Distros" TextWrapping="WrapWholeWords" />

                    <StackPanel
                        x:Name="SelectedHeader"
                        Visibility="{x:Bind GetVisibleWhen(ViewModel.HasSelection), Mode=OneWay}"
                        Spacing="4">
                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Style="{ThemeResource SubtitleTextBlockStyle}"
                                Text="{x:Bind ViewModel.HeaderName, Mode=OneWay}"
                                TextWrapping="WrapWholeWords"
                                VerticalAlignment="Center" />
                            <Border
                                Grid.Column="1"
                                Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="10,2,10,2"
                                VerticalAlignment="Center">
                                <TextBlock
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Text="{x:Bind ViewModel.HeaderState, Mode=OneWay}" />
                            </Border>
                        </Grid>

                        <TextBlock
                            Style="{StaticResource SecondaryTextStyle}"
                            Text="{x:Bind ViewModel.HeaderMetadataText, Mode=OneWay}"
                            TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </StackPanel>

                <StackPanel
                    Grid.Column="1"
                    Orientation="Horizontal"
                    Spacing="8"
                    VerticalAlignment="Top">
                    <ProgressRing Width="20" Height="20" IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" />
                    <Button
                        AutomationProperties.Name="Refresh distro page"
                        Click="OnRefreshClick">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="14" Glyph="&#xE72C;" />
                            <TextBlock Text="Refresh" />
                        </StackPanel>
                        <Button.KeyboardAccelerators>
                            <KeyboardAccelerator Key="F5" />
                        </Button.KeyboardAccelerators>
                    </Button>
                </StackPanel>
            </Grid>

            <TextBlock
                Style="{StaticResource TertiaryTextStyle}"
                Text="{x:Bind ViewModel.RefreshStatusText, Mode=OneWay}" />

            <TextBlock
                x:Name="WarningBlock"
                Visibility="{x:Bind GetVisibleWhen(ViewModel.HasWarning), Mode=OneWay}"
                Foreground="{ThemeResource SystemFillColorCautionBrush}"
                Text="{x:Bind ViewModel.WarningText, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />

            <TextBlock
                x:Name="ActionStatusBlock"
                Visibility="{x:Bind GetVisibleWhen(ViewModel.HasActionStatus), Mode=OneWay}"
                Text="{x:Bind ViewModel.ActionStatusText, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />
        </StackPanel>
        </Grid>

        <Grid
            Grid.Row="1"
            x:Name="EmptyStateContentHost"
            Visibility="{x:Bind GetVisibleWhen(ViewModel.ShowEmptyState), Mode=OneWay}"
            MaxWidth="{StaticResource WidePageContentMaxWidth}"
            HorizontalAlignment="Center"
            VerticalAlignment="Top">
            <StackPanel Width="{Binding ActualWidth, ElementName=EmptyStateContentHost}" Padding="12,12,12,32">
                <Border
                    x:Name="EmptyStateCard"
                    Style="{StaticResource CardBorderStyle}">
                    <StackPanel Spacing="8">
                        <TextBlock
                            Style="{ThemeResource BodyStrongTextBlockStyle}"
                            Text="{x:Bind ViewModel.EmptyStateTitle, Mode=OneWay}" />
                        <TextBlock
                            Text="{x:Bind ViewModel.EmptyStateMessage, Mode=OneWay}"
                            TextWrapping="WrapWholeWords" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Pivot
            Grid.Row="1"
            x:Name="DistroPivot"
            Visibility="{x:Bind GetVisibleWhen(ViewModel.HasSelection), Mode=OneWay}"
            MaxWidth="{StaticResource WidePageContentMaxWidth}"
            HorizontalAlignment="Center"
            Margin="0,8,0,0">

            <PivotItem Header="Overview">
                <ScrollViewer x:Name="OverviewScroller" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
                    <Grid x:Name="OverviewContentHost" MaxWidth="{StaticResource WidePageContentMaxWidth}" HorizontalAlignment="Center">
                    <StackPanel Width="{Binding ActualWidth, ElementName=OverviewContentHost}" Padding="12,16,12,32" Spacing="12">

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <FontIcon
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="20"
                                    Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                    Glyph="&#xEC7A;"
                                    VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Open terminal" TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Style="{StaticResource SecondaryTextStyle}"
                                        Text="Launch the default Windows Terminal session for this distro."
                                        TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <Button
                                    Grid.Column="2"
                                    AutomationProperties.Name="Open distro terminal"
                                    Click="OnOpenTerminalClick"
                                    IsEnabled="{x:Bind ViewModel.CanOpenTerminal, Mode=OneWay}"
                                    Style="{ThemeResource AccentButtonStyle}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="14" Glyph="&#xE8A7;" />
                                        <TextBlock Text="Open" />
                                    </StackPanel>
                                </Button>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <FontIcon
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="20"
                                    Glyph="&#xE768;"
                                    VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Start" TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Style="{StaticResource SecondaryTextStyle}"
                                        Text="Boot the distro VM if it is currently stopped."
                                        TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <Button
                                    Grid.Column="2"
                                    AutomationProperties.Name="Start distro"
                                    Click="OnStartClick"
                                    Content="Start"
                                    IsEnabled="{x:Bind ViewModel.CanStart, Mode=OneWay}" />
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <FontIcon
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="20"
                                    Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                    Glyph="&#xE71A;"
                                    VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Terminate" TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Style="{StaticResource SecondaryTextStyle}"
                                        Text="Immediately stop the distro and any processes running inside it."
                                        TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <Button
                                    Grid.Column="2"
                                    AutomationProperties.Name="Terminate distro"
                                    Click="OnTerminateClick"
                                    Content="Terminate"
                                    IsEnabled="{x:Bind ViewModel.CanTerminate, Mode=OneWay}" />
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid ColumnSpacing="14">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <FontIcon
                                    FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                    FontSize="20"
                                    Glyph="&#xE735;"
                                    VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Set as default" TextWrapping="WrapWholeWords" />
                                    <TextBlock
                                        Style="{StaticResource SecondaryTextStyle}"
                                        Text="Make this distro the target of plain wsl commands."
                                        TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <Button
                                    Grid.Column="2"
                                    AutomationProperties.Name="Set as default distro"
                                    Click="OnSetDefaultClick"
                                    Content="Set default"
                                    IsEnabled="{x:Bind ViewModel.CanSetDefault, Mode=OneWay}" />
                            </Grid>
                        </Border>

                        <TextBlock
                            Style="{StaticResource SecondaryTextStyle}"
                            Text="{x:Bind ViewModel.HeaderCapabilityMessage, Mode=OneWay}"
                            TextWrapping="WrapWholeWords" />
                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="Settings">
                <ScrollViewer x:Name="DistroSettingsScroller" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
                    <Grid x:Name="DistroSettingsContentHost" MaxWidth="{StaticResource WidePageContentMaxWidth}" HorizontalAlignment="Center">
                    <StackPanel Width="{Binding ActualWidth, ElementName=DistroSettingsContentHost}" Padding="12,16,12,32" Spacing="12">
                        
                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="4">
                                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Global WSL settings" TextWrapping="WrapWholeWords" />
                                    <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="{x:Bind ViewModel.Settings.GlobalWslSummary, Mode=OneWay}" TextWrapping="WrapWholeWords" />
                                    <TextBlock Style="{StaticResource TertiaryTextStyle}" Text="Settings applied at the WSL VM level affect every distro." TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="WSL Settings" Click="OnOpenWslSettingsClick" VerticalAlignment="Center" />
                            </Grid>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button
                                AutomationProperties.Name="Restore default /etc/wsl.conf"
                                Content="Defaults"
                                Click="OnRestoreDefaultsClick"
                                IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}" />
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                                <Button Content="Revert" Click="OnRevertSettingsClick" IsEnabled="{x:Bind ViewModel.Settings.IsModified, Mode=OneWay}" />
                                <Button Style="{ThemeResource AccentButtonStyle}" Content="Save" Click="OnSaveSettingsClick" IsEnabled="{x:Bind ViewModel.Settings.IsModified, Mode=OneWay}" />
                            </StackPanel>
                        </Grid>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="4">
                                    <TextBlock Style="{ThemeResource TitleTextBlockStyle}" TextWrapping="WrapWholeWords">
                                        <Run Text="Settings - " />
                                        <Run Text="{x:Bind ViewModel.HeaderName, Mode=OneWay}" />
                                    </TextBlock>
                                    <TextBlock Style="{StaticResource SecondaryTextStyle}" FontFamily="Consolas" Text="/etc/wsl.conf" TextWrapping="WrapWholeWords" />
                                    <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="{x:Bind ViewModel.Settings.StatusMessage, Mode=OneWay}" TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Top">
                                    <Button Content="Verify" Click="OnVerifySettingsClick" IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}" />
                                    <Button Content="Reload" Click="OnReloadSettingsClick" IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Structured settings" TextWrapping="WrapWholeWords" />
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.Settings.Rows, Mode=OneWay}">
                                    <ItemsRepeater.Layout>
                                        <StackLayout Spacing="8" />
                                    </ItemsRepeater.Layout>
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate x:DataType="viewmodels:DistroSettingsRowViewModel">
                                            <controls:WslConfigKeyCard ViewModel="{x:Bind}" Margin="0,0,0,8" />
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>
                        </Border>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Button
                                AutomationProperties.Name="Restore default /etc/wsl.conf"
                                Content="Defaults"
                                Click="OnRestoreDefaultsClick"
                                IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}" />
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                                <Button Content="Revert" Click="OnRevertSettingsClick" IsEnabled="{x:Bind ViewModel.Settings.IsModified, Mode=OneWay}" />
                                <Button Style="{ThemeResource AccentButtonStyle}" Content="Save" Click="OnSaveSettingsClick" IsEnabled="{x:Bind ViewModel.Settings.IsModified, Mode=OneWay}" />
                            </StackPanel>
                        </Grid>

                        <Border Style="{StaticResource CardBorderStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Raw editor" TextWrapping="WrapWholeWords" />
                                <TextBox
                                    AcceptsReturn="True"
                                    FontFamily="Consolas"
                                    MinHeight="300"
                                    Text="{x:Bind ViewModel.Settings.RawText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TextWrapping="NoWrap" />
                            </StackPanel>
                        </Border>

                        <Border x:Name="ValidationPanel" Style="{StaticResource CardBorderStyle}" Visibility="{x:Bind GetVisibleWhen(ViewModel.Settings.IsModified), Mode=OneWay}">
                            <StackPanel Spacing="8">
                                <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Validation" TextWrapping="WrapWholeWords" />
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.Settings.Errors, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate x:DataType="core:WslConfigValidationIssue">
                                            <TextBlock Foreground="{ThemeResource SystemFillColorCriticalBrush}" Text="{x:Bind Message}" TextWrapping="WrapWholeWords" />
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.Settings.Warnings, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate x:DataType="core:WslConfigValidationIssue">
                                            <TextBlock Foreground="{ThemeResource SystemFillColorCautionBrush}" Text="{x:Bind Message}" TextWrapping="WrapWholeWords" />
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                                <ItemsRepeater ItemsSource="{x:Bind ViewModel.Settings.Information, Mode=OneWay}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate x:DataType="core:WslConfigValidationIssue">
                                            <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind Message}" TextWrapping="WrapWholeWords" />
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>
                        </Border>

                        <Border x:Name="RestartPanel" Style="{StaticResource CardBorderStyle}" Visibility="{x:Bind GetVisibleWhen(ViewModel.Settings.ShowRestartPanel), Mode=OneWay}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="8">
                                    <TextBlock Style="{ThemeResource BodyStrongTextBlockStyle}" Text="Restart required" TextWrapping="WrapWholeWords" />
                                    <TextBlock Style="{StaticResource SecondaryTextStyle}" Text="Changes require restarting the distro. This takes about 8 seconds." TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Button Content="Save and terminate distro" Click="OnSaveAndTerminateClick" Style="{ThemeResource AccentButtonStyle}" />
                                    <Button Content="Save only" Click="OnSaveSettingsClick" />
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Style="{StaticResource CardBorderStyle}" Visibility="{x:Bind GetVisibleWhen(ViewModel.Settings.HasBackupPath), Mode=OneWay}">
                            <TextBlock
                                Style="{StaticResource SecondaryTextStyle}"
                                Text="{x:Bind ViewModel.Settings.BackupPath, Mode=OneWay}"
                                TextWrapping="WrapWholeWords"
                                VerticalAlignment="Center" />
                        </Border>

                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </PivotItem>

            <PivotItem Header="Diagnostics">
                <ScrollViewer x:Name="DiagnosticsScroller" VerticalScrollBarVisibility="Auto" HorizontalScrollMode="Disabled" HorizontalScrollBarVisibility="Disabled">
                    <Grid x:Name="DiagnosticsContentHost" MaxWidth="{StaticResource WidePageContentMaxWidth}" HorizontalAlignment="Stretch">
                    <StackPanel Padding="12,16,12,32" Spacing="12">

                        <Grid ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                Style="{StaticResource SecondaryTextStyle}"
                                Text="{x:Bind ViewModel.Diagnostics.StatusText, Mode=OneWay}"
                                TextWrapping="WrapWholeWords"
                                VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                <ProgressRing Width="16" Height="16" IsActive="{x:Bind ViewModel.Diagnostics.IsLoading, Mode=OneWay}" />
                                <Button
                                    AutomationProperties.Name="Refresh diagnostics"
                                    Click="OnRefreshDiagnosticsClick"
                                    IsEnabled="{x:Bind ViewModel.HasSelection, Mode=OneWay}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon FontFamily="{ThemeResource SymbolThemeFontFamily}" FontSize="14" Glyph="&#xE72C;" />
                                        <TextBlock Text="Refresh" />
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>

                        <ItemsRepeater HorizontalAlignment="Stretch" ItemsSource="{x:Bind ViewModel.Diagnostics.Results, Mode=OneWay}">
                            <ItemsRepeater.Layout>
                                <StackLayout Spacing="8" />
                            </ItemsRepeater.Layout>
                            <ItemsRepeater.ItemTemplate>
                                <DataTemplate x:DataType="diagnostics:DiagnosticResult">
                                    <Border Style="{StaticResource CardBorderStyle}">
                                        <StackPanel Spacing="8">
                                            <Grid ColumnSpacing="12">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock
                                                    Style="{ThemeResource BodyStrongTextBlockStyle}"
                                                    Text="{x:Bind Title}"
                                                    TextWrapping="WrapWholeWords"
                                                    VerticalAlignment="Center" />
                                                <Border
                                                    Grid.Column="1"
                                                    Background="{ThemeResource ControlFillColorSecondaryBrush}"
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="12"
                                                    Padding="10,2,10,2"
                                                    VerticalAlignment="Center">
                                                    <TextBlock
                                                        Style="{ThemeResource CaptionTextBlockStyle}"
                                                        Text="{x:Bind SeverityLabel}" />
                                                </Border>
                                            </Grid>
                                            <TextBlock Text="{x:Bind Summary}" TextWrapping="WrapWholeWords" />
                                            <TextBlock
                                                x:Name="DiagnosticDetailsBlock"
                                                x:Load="{x:Bind HasDetails}"
                                                Style="{StaticResource SecondaryTextStyle}"
                                                Text="{x:Bind Details}"
                                                TextWrapping="WrapWholeWords" />
                                            <TextBlock
                                                x:Name="DiagnosticSuggestedNextStepBlock"
                                                x:Load="{x:Bind HasSuggestedNextStep}"
                                                Style="{StaticResource SecondaryTextStyle}"
                                                Text="{x:Bind SuggestedNextStep}"
                                                TextWrapping="WrapWholeWords" />
                                            <TextBlock
                                                x:Name="DiagnosticCommandTextBlock"
                                                x:Load="{x:Bind HasCommandText}"
                                                FontFamily="Consolas"
                                                Style="{StaticResource TertiaryTextStyle}"
                                                Text="{x:Bind CommandText}"
                                                TextWrapping="WrapWholeWords" />
                                            <TextBox
                                                x:Name="DiagnosticRawOutputBox"
                                                x:Load="{x:Bind HasRawOutput}"
                                                AcceptsReturn="True"
                                                FontFamily="Consolas"
                                                IsReadOnly="True"
                                                MinHeight="100"
                                                Text="{x:Bind RawOutput}"
                                                TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsRepeater.ItemTemplate>
                        </ItemsRepeater>
                    </StackPanel>
                    </Grid>
                </ScrollViewer>
            </PivotItem>

        </Pivot>
    </Grid>
</Page>
